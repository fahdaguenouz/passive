package ip

// You can define an interface like:
// type Provider interface { Lookup(ip string) (ISP, City string, Lat, Lon float64, Source string, err error) }
//
// Then implement one provider (API) + maybe a mock for tests.
